# DayTrack Max API

CodeIgniter 4 API for authentication, profiles, birthdays, and the daily reminder cron endpoint.

See the project-root `README.md` for local setup. Runtime configuration belongs in the ignored `.env`; the tracked `.env.example` lists every application-specific setting.
